mysql_root_users:
  - login_user: root
    login_password: ""
    name: "{{ mysql_root_username }}"
    password: "{{ password }}"
    login_unix_socket: /var/run/mysqld/mysqld.sock
    priv: "*.*:ALL,GRANT"